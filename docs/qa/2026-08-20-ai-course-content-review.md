# AI 职业课程教材人工内容审计

- 审计日期：2026-08-20
- 审计范围：`大模型应用开发`、`RAG 知识库工程`、`Agent 开发工程师`、`AI 应用测试、部署与安全`，共 32 个 4 学时项目。
- 审计方法：逐篇核对课程边界、职业岗位任务、输入/输出/验收证据、可复现故障、官方或一手资料、安全/成本/职业规范；同时校验 `course_project_coverage.json` 的 `required_topics` 在对应 Markdown 中能够原样命中（忽略空白和大小写）。
- 审计结论：32 篇均通过。四门课的能力边界衔接清晰，每篇均有可执行产物、验收证据、至少两个完整故障案例、经日期标注的官方/一手来源及安全与职业责任说明。

## 逐项审计记录

| 课程 / 项目 | 边界与岗位任务 | 产物与验收 | 故障案例 | 官方/一手来源 | 安全、成本与职业规范 | 结论 |
|---|---|---|---|---|---|---|
| LLM / `model-client` | 仅封装单次 OpenAI 兼容模型请求，明确排除检索、自主循环和多智能体；岗位任务是将外部推理接口封装成边界稳定的内部客户端。 | `ModelClient`、配置示例、自动化检查和脱敏运行记录；对成功、配置缺失、401/429/超时和请求标识有明确证据。 | 完整端点重复追加、读取超时无上限重试、HTTP 200 却是工具请求三类典型问题，现象/原因/定位/修复完整。 | OpenRouter API、HTTPX 超时、OpenAI API，标注 2026-08-20。 | 密钥仅在服务端环境，日志不记鉴权值；覆盖数据授权、去标识化、重试费用与能力降级责任。 | 通过 |
| LLM / `prompt-template` | 仅做单次调用前的提示构造和回归，不做 RAG 或 Agent；任务是将业务提示转成可版本化、可测试资产。 | `feedback-v1` 规范、安全渲染器、五条固定样例和变更记录；覆盖变量校验、角色分离、版本/哈希与回归阈值。 | 改版后信息不足时编造、花括号二次解释、线上版本不可追溯，均给出回归方法。 | OpenAI Prompting Guide/API 兼容性与 OpenRouter 消息结构，标注 2026-08-20。 | 强调真实学员数据去标识、高影响反馈人工复核、分隔符不等于权限控制，不宣称绝对防注入。 | 通过 |
| LLM / `structured-output` | 限定为单次结构化提取，不将修复循环扩展为 Agent；任务是建立语法、Schema、业务和事实证据四层契约。 | 岗位信息 Schema、提取服务、降级说明和六条测试；额外字段、未知值、能力降级和有限修复可验收。 | 模型不支持严格 Schema、类型合格但事实编造、修复循环不断消耗费用。 | OpenRouter Structured Outputs、Pydantic JSON Schema/Models，标注 2026-08-20。 | 覆盖联系人信息删减、不执行模型 URL/命令、原文保留策略与人工复核；不把 Schema 合格等同事实正确。 | 通过 |
| LLM / `context-budget` | 属于应用层会话预算，明确不实现长期记忆检索或 Agent 决策；任务是同时守住关键约束、窗口和单位成本。 | `ContextBudget`、裁剪器、版本化摘要和成本表；覆盖三档会话、摘要事实核对、估算/实际差异及成本维度。 | 裁剪掉固定规则、本地估算未超限仍被拒绝、摘要把计划写成已完成。 | OpenRouter usage/参数与 OpenAI tiktoken，标注 2026-08-20。 | 区分上下文裁剪和真实删除，覆盖去标识、派生摘要删除、成本/延迟/质量联合评估。 | 通过 |
| LLM / `multimodal-input` | 限定低风险教学图片，不延伸到 CV/OCR 训练或 Agent；任务是建立上传、解码、标准化、能力检查和人工复核边界。 | 上传校验器、图片标准化函数、多模态请求构造器与结构化读数；五类本地样本可复现。 | 假 JPG/损坏字节、EXIF 方向颠倒、文本模型伪称看图三类故障。 | OpenRouter 多模态/图像输入与 Pillow 格式安全文档，标注 2026-08-20。 | 检查人脸/工牌/位置等个人信息、EXIF、SSRF 和解析器资源限额；安全读数必须由有资质人员核验。 | 通过 |
| LLM / `tool-calling` | 限定单轮、只读工具闭环，不引入自主规划或写操作；任务是分离“模型建议”与“应用执行”。 | 课程查询工具、注册表、审计事件和单轮编排函数；未知工具、参数、权限和执行状态均可验证。 | 未知工具通过反射执行、审计早于真实查询、结构合法但对象级越权。 | OpenRouter Tool Calling/API 与 Pydantic Models，标注 2026-08-20。 | 覆盖最小权限、白名单、对象授权、人类确认/幂等/补偿；准确区分 requested 与 executed。 | 通过 |
| LLM / `streaming-interaction` | 只优化应用流式交互，明确不展示或伪造模型思维链；任务是统一上游 SSE、浏览器状态和端到端取消。 | 流式代理、事件契约、前端消费示例、取消通道和测试；唯一终态、跨块 UTF-8、断线与降级可验收。 | UTF-8 拆块乱码、页面取消但上游继续计费、流内错误无终态。 | OpenRouter Streaming/API、MDN SSE/AbortController，标注 2026-08-20。 | 不向前端暴露原始错误和提示，取消必须释放上游；高风险内容先整体校验，失败内容不作为学习事实。 | 通过 |
| LLM / `capstone` | 组合前七个应用能力，明确排除自主规划、长期记忆、完整 RAG 和多智能体；岗位任务是交付边界清晰的质检 MVP。 | 可运行 API 服务、配置、测试套件、演示数据和交付文档；结构化、图片能力、工具与流式链路联合验收。 | 备用文本模型虚构图片、工具失败却保存正式报告、干净环境不可启动。 | OpenRouter API、Structured Outputs、Multimodal、Tool Calling 和 Streaming，标注 2026-08-20。 | 整条派生数据链保持访问/删除一致，密钥和内部响应不外泄；以单位合格报告评估成本，如实说明 MVP 与人工复核边界。 | 通过 |
| RAG / `requirements` | 只建立 RAG 需求基线，不编写切分或向量代码；任务是将模糊愿望拆成问题类型、语料边界、权限、拒答与可签收指标。 | 需求说明、来源登记、20 条问题集和风险表；每问关联来源/证据或拒答，且有范围与风险签字。 | 小样本准确但真实问法失配、引用废止规范、正确拒答被计为错误。 | RAG 原始论文、OpenAI Retrieval Guide、教育部人工智能专业教学标准，标注 2026-08-20。 | 覆盖数据最小化、版权/许可、检索前权限与高风险版本/责任人提示；未实测阈值不伪造。 | 通过 |
| RAG / `document-pipeline` | 终点是可追溯的标准化 Document，不提前选块长或建索引；岗位任务是将 Markdown/PDF/HTML 变成可重建、可隔离的文档数据。 | Document 模型、三个解析适配器、manifest、隔离报告和 JSONL；位置回溯、两次重建一致性与异常来源隔离可验收。 | 扫描 PDF 当空文档、页眉去重删除正文、移动文件使引用失效。 | pypdf、Python-Markdown 与 Beautiful Soup 官方文档，标注 2026-08-20。 | 限制不可信文件的字节/页数/资源，路径 resolve 后验证根目录，遵守网页许可和原数据权限；如实披露 OCR/表格限制。 | 通过 |
| RAG / `chunking` | 只形成可独立理解、可引用的证据单元，不训练 embedding 或在正文堆测试词；任务是结构/标题感知切分。 | `StructureAwareChunker`、策略配置、块清单和对比报告；稳定 ID、面包屑、行/页位置、结构完整与人工抽查有证据。 | 否定条件与步骤分离、表格数值丢失列名、重叠块占满搜索结果。 | Unstructured Chunking、OpenAI Retrieval Guide、Python Markdown，标注 2026-08-20。 | 严禁跨权限/版本合并，重叠引用回到唯一原位置；同时比较质量、embedding 成本与失败分布。 | 通过 |
| RAG / `retrieval-index` | 分别建立 BM25 与向量候选召回，明确本项目不做融合和重排；任务是持久化索引、原子发布与源材料一致性校验。 | BM25Index、VectorIndex、索引 manifest、构建 CLI 与统一 SearchHit；覆盖课程/权限过滤、重启、损坏、回滚与六问 Top-5。 | 专业编号被分词、向量命中映射错段、材料更新却加载旧索引。 | Elasticsearch BM25、Faiss、OpenAI Embeddings Guide，标注 2026-08-20。 | 索引/嵌入按源数据等级保护，过滤信任服务端身份，删除覆盖派生数据；先离线基线再授权远程 embedding。 | 通过 |
| RAG / `retrieval-optimization` | 仅优化候选证据，不生成答案；任务是在统一过滤后做 RRF、小候选重排与受控查询改写。 | HybridRetriever、RRF、可选 reranker、诊断日志和十问对比报告；保留原查询、分路权限与超时降级。 | 原始分数相加导致单路支配、改写丢失否定/版本、重排超时导致空结果。 | Qdrant Hybrid/RRF/Reranking 与 Elasticsearch RRF，标注 2026-08-20。 | 每条候选路径在融合前执行权限，外部重排须有数据授权；同时报告 Recall@5、P95、调用率与高风险子集。 | 通过 |
| RAG / `grounded-generation` | 只基于已过滤候选生成一次答案，不构建 Agent；任务是稳定引用 ID、主张证据覆盖、冲突/歧义/无证据拒答。 | ContextAssembler、GroundedAnswer Schema、引用校验器和十条样例；来源 ID、路径/行页、主张支持与 follow-up 可验收。 | 引用存在却不支持数字、新旧版本合并、文档伪指令改变输出。 | RAG 原始论文、OpenAI Retrieval Guide、OWASP Prompt Injection Cheat Sheet，标注 2026-08-20。 | 引用仅显示有权来源，检索文本与 query 均为不可信数据；高风险内容显示版本并由教师确认，不以链接数量代替依据质量。 | 通过 |
| RAG / `rag-evaluation` | 建立多层评测与风险门槛，不用单一模型裁判代替人工责任；任务是将语料、召回、排名、依据答案、拒答和性能纳入回归。 | 版本化评测 JSONL、运行器、指标报告、失败分类与发布模板；30 问分层数据、中间结果和人工分歧可复玩。 | 平均 Recall@5 上升但高风险退化、模型裁判给伪引用高分、修改冻结集导致候选假通过。 | RAGAS/BEIR 原始论文与 scikit-learn Ranking Metrics，标注 2026-08-20。 | 评测问题去标识、越权测试使用虚构数据、外部裁判须授权；高风险结论由业务责任人审核。 | 通过 |
| RAG / `capstone` | 只交付单知识库问答服务，不引入自主规划、多 Agent 或外部工具自动执行；任务是将全条 RAG 构建/查询路径交付成可运行 MVP。 | 知识库服务、材料清单、构建/查询 CLI、索引、测试和手册；覆盖课程隔离、引用路径、索引状态、30 问评测报告和盲验。 | stale 时偷调通用模型、一课更新使全课不可用、引用路径逃出课程目录。 | RAG 原始论文、OpenAI Retrieval、Qdrant Hybrid、RAGAS 原始论文，标注 2026-08-20。 | 课程/权限隔离是程序硬边界，索引/日志/评测/缓存延续源权限；不暴露绝对路径，远程能力按授权和预算开启。 | 通过 |
| Agent / `agent-loop` | 只实现不依赖特定框架的单 Agent 顺序执行循环；任务是由程序控制步数、超时、允许行动和真实终态。 | `AgentRunner`、结构化事件、终态与用户结果；正常、步数上限、取消、工具失败四组可重放测试。 | 重复调用同一工具、模型声称完成但产物不存在。 | OpenAI Agent 实践指南、Google Agents CLI 教程，标注 2026-08-20。 | 限制步数/时间/预算，默认确认写操作，日志脱敏，取消后禁止新副作用；明确告知未完成原因。 | 通过 |
| Agent / `tool-system` | 仅建设模型与真实系统之间的受控工具层；任务是将宽能力拆成窄契约，完成参数/身份/权限验证、幂等和审计。 | `ToolSpec`、注册表、受控分发器、统一结果与审计日志；合法读取、非法字段、越权、确认、超时重试和幂等去重六组证据。 | 超时后重复创建草稿、模型参数越权读取其他学生。 | OpenAI Agent 指南、MCP Tools 规范、OWASP Excessive Agency，标注 2026-08-20。 | 把模型参数、MCP 注解和工具返回视为不可信；限制 URL/SQL/路径/命令，高风险操作可见确认，设定调用/并发/输出上限。 | 通过 |
| Agent / `memory` | 建立状态、会话历史、派生摘要和受控长期记忆的分层系统；任务是由应用决定写入、取回、更正、过期和删除。 | 会话/长期仓库、摘要器接口、选择器和更正/删除；同用户续学、跨课/跨用户隔离、更正、过期与恢复六组证据。 | 恢复会话混入其他课程、用户更正偏好后旧记忆仍被采用。 | Google ADK Sessions and Memory、OpenAI Agent 指南，标注 2026-08-20。 | 长期记忆按用途/保留期收集，提供查看、更正、删除，读取时强制隔离；评分/资格不依据模型摘要。 | 通过 |
| Agent / `workflow` | 把确定性业务规则与模型节点分离成状态工作流；任务是明确节点契约、分支、并行、有上限循环、人工确认与检查点恢复。 | 状态模型、节点注册表、执行器、检查点仓库和五组路径测试；强制允许边、循环上限与恢复幂等。 | 并行节点互相覆盖状态、恢复后重复发布训练方案。 | Google ADK Workflow Agents、OpenAI Agent 指南，标注 2026-08-20。 | 确定性规则不交给模型绕过，审批页明示对象/副作用，检查点无明文密钥；并发受预算限制，失败不冒充完成。 | 通过 |
| Agent / `knowledge-tools` | 明确不重做向量检索或数据库，只将已有 RAG、档案与预约 API 封装为知识/业务工具；任务是跨服务契约与权限集成。 | 三个工具 Schema/适配器、权限矩阵、模拟服务、自动化测试和运行记录；引用、越权、确认、重试与 run_id 审计可验收。 | 引用存在但无法打开、网络超时导致重复预约。 | OpenAI Agent 指南、OWASP Excessive Agency、Python `unittest.mock`，标注 2026-08-20。 | 密钥不进入工具描述/模型/前端，学员档案字段级脱敏且执行层隔离；缓存以权限主体为键，区分服务无结果与模型不知。 | 通过 |
| Agent / `mcp-integration` | 只实现最小 MCP 客户端接入；明确 MCP 不自动解决授权、业务确认和真实性，Host 仍掌握权限和编排。 | 连接配置、能力协商器、工具桥、确认策略、模拟 Server 与测试；握手版本、allowlist、资源 URI/大小、断线和能力变更均可验收。 | 服务器在线但因版本/能力协商失败而无工具、恶意资源文本诱导 Agent 执行无关指令。 | MCP 架构、Server Tools 与授权规范，版本 2025-06-18、教材核验 2026-08-20。 | MCP Server 是外部信任边界，覆盖发布者/代码/更新核验、低权限本地进程、批准 HTTPS、令牌隔离、能力最小化与用户可取消。 | 通过 |
| Agent / `multi-agent` | 不把同一模型简单复制；仅当角色工具/规则差异足够大时拆分，确定性分类仍用程序。 | 协作拓扑、角色卡、handoff Schema、编排代码、冲突/预算策略与基线对比；路由、越权、移交循环、超时与聚合均可验收。 | 两个角色循环移交、汇总答案遗漏安全角色的关键风险。 | OpenAI Agent 指南、Google ADK Workflow Agents/Sessions，标注 2026-08-20。 | 每角色真实工具最小权限，个人数据跨角色用摘要/引用，高影响结果可申诉/人工复核；限制团队 Token、时间和工具预算。 | 通过 |
| Agent / `capstone` | 综合交付可观测、可中断、含人工确认的职业学习助理；任务是从验收反推单 Agent/工作流/必要专业角色和安全恢复。 | 架构/威胁模型、可运行应用、配置、测试、追踪报告、用户说明和演示；二十个场景覆盖主题一致、引用、取消、确认、幂等与恢复。 | 教材更新后仍追问旧知识点、用户取消后仍写任务、恢复运行重复消费旧确认。 | OpenAI Agent 指南、MCP 架构、OWASP Excessive Agency、Google ADK Sessions，标注 2026-08-20。 | 模型输入/工具/外部资源最小权限与限额，学生档案最小使用、日志脱敏和可人工纠正；模型评分不直接用于处分/资格，并披露 MVP 与生产运维差距。 | 通过 |
| Production / `quality-standard` | 只制定质量决策契约，不提前实现测试或部署；岗位任务是把业务风险转换为指标、阈值、证据和发布责任。 | 质量模型、风险分级、指标字典、最小验收集和门禁配置；高风险独立阻断、证据不足与评审签字可验收。 | 综合分高却放行危险版本、评审者结论相反，均给出指标拆分与校准修复。 | NIST AI RMF、NIST Generative AI Profile 与 AIRC TEVV，标注 2026-08-20。 | 覆盖样本偏差、指标副作用、例外期限与责任人；不以移动阈值或删除失败样本换取发布。 | 通过 |
| Production / `functional-testing` | 聚焦应用可控的确定性契约，将开放式语义评测留到下一项目；岗位任务是建立快速、隔离、可定位的自动化测试。 | 测试目录、替身、固定样本、覆盖矩阵、脚本和报告；课程身份、索引护栏、权限、状态与 SSE 契约有明确断言。 | 单测独立通过但全量随机失败、mock 调用断言与真实注入对象错位。 | pytest、Python `unittest.mock`、FastAPI Testing 与 OpenAI Evaluation Best Practices，标注 2026-08-20。 | 模型与外部服务默认使用替身，测试数据去标识，缺陷注入证明关键回归能真实阻断发布。 | 通过 |
| Production / `effect-evaluation` | 评测特定职业学习任务而非抽象模型智力；任务是用规则、人工量表和受控模型评审比较候选版本。 | 分层数据集、量表、评审脚本、人工校准、比较报告和失败清单；样本、上下文版本、原始输出与裁决可复现。 | 开发集泄漏导致虚假提升、总体稳定却有课程切片退化。 | NIST AIRC/AI RMF、OpenAI Evals 与评测最佳实践，标注 2026-08-20。 | 开发集与保留集隔离，候选盲化，模型裁判不能单独做高风险结论，完整呈现不确定性。 | 通过 |
| Production / `observability` | 建立指标、trace、日志的关联体系，不把“接入 SDK”当完成；岗位任务是依据 run_id 定位模型、检索、工具与状态链路。 | span 拓扑、遥测字段、SLO、仪表板和故障注入报告；慢请求、错误与 Token 成本能回到具体阶段。 | 告警升高却找不到 trace、高基数标签使监控费用和查询延迟暴涨。 | OpenTelemetry Primer、Signals、Traces 与 Logs 规范，标注 2026-08-20。 | 正文与学生档案不进入普通遥测，采样优先保留高价值失败，观测系统本身也有健康与成本边界。 | 通过 |
| Production / `ai-security` | 以信任边界和确定性授权防御提示注入与越权，不承诺字符串过滤能绝对安全；任务是建立纵深控制链。 | 威胁模型、权限矩阵、输入输出策略、确认流程、对抗集和安全报告；未授权与跨课请求真实工具调用数为零。 | 恶意教材诱导工具执行、安全规则误伤正常学习问题。 | OWASP GenAI/LLM Top 10、NIST Generative AI Profile、MCP Security Best Practices，标注 2026-08-20。 | 身份由服务端注入，工具最小权限、参数变更使确认失效；攻击样本与漏洞信息限制访问并遵循披露流程。 | 通过 |
| Production / `performance-cost` | 端到端拆分排队、检索、首字、生成、工具与持久化；岗位任务是在质量安全门禁不退化的前提下降低 P95 和成功任务成本。 | 基准、预算、并发/限流策略、优化实验与容量报告；相同负载和样本下可比较质量、延迟与费用。 | 重试放大失败和费用、缓存键缺课程/权限导致跨课答案。 | OpenAI Latency/Cost Optimization、Python asyncio 与 Kubernetes 资源管理，标注 2026-08-20。 | 缓存与路由不能降低课程隔离和安全校验，有限重试要求幂等，容量不足需明确背压和降级。 | 通过 |
| Production / `deployment` | 将已验收应用变成可信、可重复、可恢复的制品；任务是完成容器、探针、持续交付与数据兼容回滚。 | 镜像、运行契约、健康检查、流水线、发布和回滚记录；非 root、就绪探针、优雅停机及核心冒烟可验证。 | 容器 healthy 但课程请求失败、镜像回滚后旧版无法读取会话。 | Dockerfile/构建最佳实践、Kubernetes Probes、GitHub Actions 安全强化，标注 2026-08-20。 | 密钥不进入镜像，制品用摘要固定，最小基础镜像与依赖审查；回滚必须连同状态与数据兼容一起演练。 | 通过 |
| Production / `capstone` | 不重复开发 Agent，而是将前七项能力串成一次真实上线验收；交付负责人可依据证据发布、限范围或拒绝发布。 | 冻结候选制品、生产就绪清单、测试/评测/安全/性能/观测报告及发布回滚记录；每阶段有进入和停止条件。 | 子报告各自通过但版本/阈值不一致无法签字、回滚恢复错误率却丢学习状态。 | NIST AI RMF/Profile、OpenTelemetry、OWASP GenAI 与 Docker，标注 2026-08-20。 | 用同一制品和数据链审计决定，故障演练验证真实恢复；如实记录剩余风险，停止发布同样是合格职业行为。 | 通过 |
